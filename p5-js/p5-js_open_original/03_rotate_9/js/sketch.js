"use strict";
//==========
// p5.js

let palette = ["#386641", "#6A994E", "#A7C957", "#F2E8CF", "#BC4749"];
let cX, cY;

function setup(){
	createCanvas(windowWidth, windowHeight);
	angleMode(DEGREES);
	frameRate(16);
	noFill();
	noStroke();

	cX = width / 2;
	cY = height / 2;
}

function draw(){
	background(33);

	
}

function drawPattern(x, y, rot){

	push();


	pop();
}